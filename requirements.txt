# Pinned dependency set.
# Version matrix (do not change one without the others):
#   Python 3.11 · Java 17 (Temurin) · Spark 3.5.3 · Scala 2.12 · Sedona 1.9.0
# Matching Spark jars are declared in config.py (SPARK_PACKAGES).

# --- Core engine -------------------------------------------------------------
pyspark==3.5.3

# --- Geospatial (Sedona 1.9.0 pairs with geotools-wrapper 1.9.0-33.5) --------
apache-sedona==1.9.0
geopandas>=1.0,<2.0
shapely>=2.0,<3.0

# --- Data / IO ---------------------------------------------------------------
pandas>=2.1,<3.0
pyarrow>=15.0
requests>=2.31

# --- Streaming client (producer.py writes to Kafka without Spark) ------------
kafka-python-ng>=2.2.3

# --- Features / calendar -----------------------------------------------------
holidays>=0.50

# --- Visualisation (maps, elbow & silhouette curves, heatmap) ----------------
folium>=0.16
matplotlib>=3.8
